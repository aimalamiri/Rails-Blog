<nav class="bg-white w-full px-20 py-8 flex items-center gap-5">
  <a href="<%= users_path %>" class="font-bold text-gray-600 hover:opacity-80">All Users</a>
  <a href="<%= new_user_post_path(user_id: current_user.id) %>" class="font-bold text-gray-600 hover:opacity-80">New Post</a>
</nav>
